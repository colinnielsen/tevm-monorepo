export * from './toBaseError.js'
export * from './toTaggedError.js'

/**
 * Re-export type definitions for toTaggedError.
 * TypeScript users should see proper type narrowing via the overloads in toTaggedError.types.ts
 *
 * @see {@link ./toTaggedError.types.ts} for TypeScript overload signatures that provide type narrowing
 */
